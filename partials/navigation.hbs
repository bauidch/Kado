<div class="nav-b">
</div>

<div class="kado-slider">
    <h1>{{@blog.title}}</h1>
    <h2 class="subheader">{{@blog.description}}</h2>
</div>

      <nav class="top-bar">
        <section class="top-bar-section">
          <ul class="left">
            {{#foreach navigation}}
                <li><a href="{{url absolute="true"}}">{{label}}</a></li>
            {{/foreach}}
          </ul>
        </section>
      </nav>
